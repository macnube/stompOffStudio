const styles = theme => ({
    toolbar: {
        display: 'flex',
        justifyContent: 'flex-end',
        marginBottom: theme.spacing(1),
    },
    action: {
        paddingLeft: theme.spacing(1),
    },
});

export default styles;
