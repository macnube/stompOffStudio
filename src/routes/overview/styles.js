const styles = theme => ({
    root: {
        display: 'flex',
    },
    chartContainer: {
        marginLeft: -22,
    },
    tableContainer: {
        height: 320,
    },
});

export default styles;
